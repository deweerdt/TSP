# Makesubdirs


all debug clean print config::
	@for dir in $(SUBDIRS); \
	 do \
		(cd $$dir; $(MAKE) "LEVEL=$$LEVEL---" $@); \
		if [ $$? -ne 0 ]; then \
			break; \
		fi; \
	 done

# less verbosity
#		echo ">>>$(LEVEL) BEGIN (`pwd`/$$dir) [$@]"; \
#		echo ">>>$(LEVEL) END (`pwd`/$$dir)"; \
