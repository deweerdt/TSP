# Makesubdirs


build debug destroy print config::
	@for dir in $(SUBDIRS); \
	 do \
		(cd $$dir; $(MAKE) -s "LEVEL=$$LEVEL---" $@); \
		if [ $$? -ne 0 ]; then \
			exit 1; \
		fi; \
	 done

# less verbosity

#		echo ">>>$(LEVEL) END (`pwd`/$$dir)"; \
