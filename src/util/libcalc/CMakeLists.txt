SET(LIBCALC_SRC calc_func)

INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/src/util/libcalc)
ADD_LIBRARY(tsp_calc ${LIBCALC_SRC})

IF (WIN32)
    TARGET_LINK_LIBRARIES(tsp_calc )
ELSE (WIN32)
    TARGET_LINK_LIBRARIES(tsp_calc m)
ENDIF (WIN32)
TSP_LIBRARY_INSTALL(tsp_calc)
TSP_XINCLUDE(${LIBCALC_SRC})
